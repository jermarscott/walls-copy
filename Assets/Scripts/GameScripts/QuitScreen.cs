
using UnityEngine;
using UnityEditor;

public class QuitScreen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {


        /*
        //QUIT GAME SETUP
        if (Input("Quit"))
        {

            EditorUtility.DisplayDialog("Place Selection On Surface?",
               "Are you sure you want to place " + transforms.Length
               + " on the surface?", "Quit", "Do Not Quit");

        }
        if (Input("Quit"))
        {
            Application.Quit();
        }
        */
    }
}
